import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    width: '100%',
    backgroundColor: '#F00',
    flexDirection: 'row',
    justifyContent: 'space-between'
  }
});